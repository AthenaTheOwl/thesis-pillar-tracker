# Acceptance - 0002

## Done Means

- `uv run pytest` passes.
- `python scripts/voice_lint.py` passes.
- `python scripts/spec_check.py` passes.
- `python scripts/validate_pillar_schema.py` passes.
- `uv run tpt --help` lists all v0.1 subcommands.
- `monthly_reviews/2026-06.md` references the six starter pillars.

## Manual Review

A reviewer should read `monthly_reviews/2026-06.md` and confirm that it is a
neutral scaffold report. Public-event backfill belongs to the next run.

